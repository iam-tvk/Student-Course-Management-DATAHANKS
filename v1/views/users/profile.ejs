<html>

<head>
    <title>Datahanks</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>

<body>
    <nav class="navbar navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/"><span class="glyphicon glyphicon-hdd" aria-hidden="true"></span>Datahanks</a>
            </div>

            <div class="collapse navbar-collapse">
                <ul class=" nav navbar-nav navbar-right">
                    <% if(!currentUser){ %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/register">Signup</a></li>
                        <% } else { %>
                            <li> <a href="/users/Profile">Currently Logged in as <%=currentUser.username
                                %></a> </li>
                            <li><a href="/logout">Logout</a></li>
                            <% } %>

                </ul>

            </div>
        </div>
    </nav>
    <div class="container">
        <h1>My Profile</h1>
        <Div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h1>This is me:)</h1>
                    <% if (locals.user) { %>
                        <img src="<%= get.currentUser.avatar  %> " alt="">
                        <p>
                            <%=currentUser.username %>
                        </p>
                        <h1>wwe</h1>
                        <% } %>
                </div>
                <div class="col-md-8"></div>
            </div>
        </Div>
    </div>



</body>

</html>